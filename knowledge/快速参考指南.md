# 快速参考指南

> 常用命令、配置模板和故障排查速查表

---

## 🚀 5分钟部署流程

```bash
# 1. 初始化Git仓库
git init
git add .
git commit -m "Initial commit"

# 2. 推送到GitHub
git remote add origin https://github.com/username/repo.git
git push -u origin main

# 3. 部署到Vercel
# 访问 vercel.com，导入GitHub仓库，点击Deploy

# 4. 配置Cloudflare DNS（灰色云朵！）
# CNAME: www → cname.vercel-dns.com
# A记录: @ → 76.76.21.21
# Proxy status: DNS only（灰色云朵）

# 5. 修改Nameserver到Cloudflare
# 在域名注册商处修改为Cloudflare提供的NS地址
```

---

## 📋 vercel.json 配置模板

### 静态网站（推荐）
```json
{
  "cleanUrls": true,
  "trailingSlash": false
}
```

### 带缓存头
```json
{
  "cleanUrls": true,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=3600"
        }
      ]
    }
  ]
}
```

---

## 🌐 Cloudflare DNS 配置模板

### 标准配置（推荐）
```
类型: CNAME
名称: www
目标: cname.vercel-dns.com
代理: 🟡 DNS only（灰色云朵）

类型: A
名称: @
IPv4: 76.76.21.21
代理: 🟡 DNS only（灰色云朵）
```

### SSL设置
```
SSL/TLS → Overview
选择: Full (strict)
```

---

## 🛠️ 常用命令

### Git命令

```bash
# 查看状态
git status

# 提交更改
git add .
git commit -m "描述"

# 推送到GitHub
git push

# 查看远程仓库
git remote -v

# 修改远程仓库地址
git remote set-url origin https://new-url.git
```

### DNS检查命令

```bash
# Windows
nslookup www.yourdomain.com

# Mac/Linux
dig www.yourdomain.com
dig @8.8.8.8 www.yourdomain.com  # 使用Google DNS
```

### 本地服务器

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js
npx serve

# PHP
php -S localhost:8000
```

---

## 🐛 快速故障排查

### 问题：网站打不开

**检查清单**：
1. ✅ DNS记录是否正确
2. ✅ Nameserver是否指向Cloudflare
3. ✅ Cloudflare状态是否为Active
4. ✅ Vercel部署状态是否为Ready

**解决方案**：
```bash
# 清除浏览器缓存
# Windows: Ctrl + Shift + R
# Mac: Cmd + Shift + R

# 清除Cloudflare缓存
# Cloudflare → Caching → Purge Cache
```

---

### 问题：功能异常

**检查清单**：
1. ✅ 打开浏览器控制台（F12）
2. ✅ 查看Console标签的错误信息
3. ✅ 检查Network标签的资源加载
4. ✅ 测试不同浏览器的兼容性

**常见错误**：
- `404 Not Found` - 文件路径错误
- `CORS error` - 跨域问题
- `Unexpected token` - JavaScript语法错误

---

### 问题：国内访问慢

**检查清单**：
1. ✅ Cloudflare使用的是灰色云朵吗？
2. ✅ DNS记录配置正确吗？
3. ✅ SSL模式是Full (strict)吗？

**优化方案**：
- 使用国内CDN（阿里云、腾讯云）
- 优化图片和资源
- 启用缓存

---

## 📱 移动端优化清单

### 触摸优化
```css
.option {
    min-height: 48px;  /* 最小触摸目标 */
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
}
```

### 响应式断点
```css
/* 小屏手机 */
@media (max-width: 375px) { }

/* 中等手机 */
@media (min-width: 376px) and (max-width: 414px) { }

/* 大屏手机 */
@media (min-width: 415px) { }
```

### 字体大小
```css
/* 避免iOS自动缩放 */
input, select, button {
    font-size: 16px;
}
```

---

## 🎨 常用代码片段

### 禁用选项焦点残留
```javascript
function renderPage() {
    const app = document.getElementById('app');
    app.innerHTML = '';

    // 清除焦点
    if (document.activeElement) {
        document.activeElement.blur();
    }
    document.body.focus();

    // 渲染页面...
}
```

### 选项选中效果
```css
.option {
    background: white;
    border: 2px solid #E0E0E0;
    transition: all 0.2s ease;
}

.option.selected {
    background: var(--color-rice);
    border-color: var(--color-vermilion);
}

.option:active {
    transform: scale(0.98);
}
```

### 防止表单默认行为
```css
input:focus,
select:focus,
button:focus {
    outline: none;
}
```

---

## 📊 性能优化清单

### 图片优化
- 使用WebP格式
- 压缩图片大小
- 使用响应式图片
- 懒加载

### 代码优化
- 压缩CSS和JavaScript
- 移除未使用代码
- 使用代码分割
- 延迟加载

### 缓存策略
```json
{
  "headers": [
    {
      "source": "/css/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=86400"  // 24小时
        }
      ]
    }
  ]
}
```

---

## 🔧 .gitignore 模板

```gitignore
# 操作系统
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 测试文件
test_*.html
test_*.js
preview-*.html

# 日志
*.log

# 临时文件
*.tmp
*.temp

# 文档
*.md
!README.md
```

---

## 📞 紧急联系

### 服务状态页面
- **Vercel**: https://www.vercel-status.com/
- **Cloudflare**: https://www.cloudflarestatus.com/
- **GitHub**: https://www.githubstatus.com/

### 获取帮助
- Vercel文档: https://vercel.com/docs
- Cloudflare文档: https://developers.cloudflare.com/
- MDN Web Docs: https://developer.mozilla.org/

---

## ✅ 部署前检查清单

### 代码检查
- [ ] 移除所有console.log
- [ ] 移除测试文件
- [ ] 确认资源路径正确
- [ ] 检查JavaScript语法

### 功能检查
- [ ] 所有链接正常工作
- [ ] 表单提交正常
- [ ] 移动端显示正常
- [ ] 不同浏览器兼容

### 性能检查
- [ ] 图片已优化
- [ ] 代码已压缩
- [ ] 启用了缓存
- [ ] 加载速度可接受

### 安全检查
- [ ] 使用HTTPS
- [ ] 设置安全头
- [ ] 验证用户输入
- [ ] 防止XSS攻击

---

**最后更新**: 2025-02-08
